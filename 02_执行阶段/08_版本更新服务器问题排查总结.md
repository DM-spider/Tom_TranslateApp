# 版本更新 —— 服务器问题排查总结

> **日期**：2026-04-06
> **背景**：完成 Phase 1（登录功能 + 付费计划 基础改造）后，将新代码部署到服务器时遇到的一系列问题。

---

## 问题总览

| # | 问题 | 现象 | 根因 | 严重程度 |
|---|------|------|------|----------|
| 1 | 注册接口 500 | `POST /api/v1/auth/register` 返回 `Internal Server Error` | `passlib` 与 `bcrypt>=4.0` 不兼容 | 🔴 严重 |
| 2 | 网页 502 | 浏览器访问 `tomtranslate.com` 返回 502 Bad Gateway | 前端 (Next.js, port 3000) 未启动 | 🔴 严重 |
| 3 | 后端端口占用 | `pnpm run dev` 时后端报 `[Errno 98] Address already in use` | 旧的 uvicorn 进程未退出，仍占用 8000 端口 | 🟡 中等 |
| 4 | Git pull 冲突 | `git pull` 时报 `Your local changes would be overwritten` | 服务器上有未提交的本地修改 (`alembic/env.py`) | 🟡 中等 |

---

## 问题详解与解决方案

### 问题 1：注册接口 500 —— passlib 与 bcrypt 版本不兼容

**现象**：

```bash
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"123456"}'
# → Internal Server Error
```

**日志关键报错**：

```
(trapped) error reading bcrypt version
AttributeError: module 'bcrypt' has no attribute '__about__'
```

**根因**：`passlib` 库已很久未更新，与 `bcrypt >= 4.0.0` 不兼容。新版 bcrypt 移除了 `__about__` 模块，导致 passlib 调用失败。

**解决方案（二选一）**：

- **临时方案**：降级 bcrypt 版本

  ```bash
  cd /root/Tom_TranslateApp/packages/backend
  uv pip install "bcrypt<4.0"
  # 验证：bcrypt==3.2.2 安装成功
  ```

- **长期方案**：移除 passlib 依赖，直接使用 bcrypt 库进行密码哈希（已在本地代码中完成，需要部署到服务器）

**教训**：升级依赖前需要检测间接依赖的兼容性。

---

### 问题 2：网页 502 —— 前端服务未启动

**现象**：浏览器访问 `tomtranslate.com` 返回 502 Bad Gateway。

**排查过程**：

```bash
# 1. 确认后端正常
curl http://localhost:8000/health
# → {"status":"ok"}  ✅

# 2. 检查 nginx 配置，发现前端走 port 3000
cat /etc/nginx/conf.d/*.conf
# location / { proxy_pass http://127.0.0.1:3000; }

# 3. 检查前端进程
ps aux | grep node
# → 没有 node 进程  ❌

# 4. 查看 nginx 错误日志确认
tail -30 /var/log/nginx/error.log
# → connect() failed (111: Connection refused) ... upstream: "http://127.0.0.1:3000/"
```

**根因**：前端 Next.js 服务（port 3000）没有启动，Nginx 反向代理无法连接。

**解决方案**：

```bash
# 启动前端服务
cd /root/Tom_TranslateApp
pnpm run dev &
# 前端启动后：Next.js 15.5.12 - Local: http://localhost:3000 ✅
```

**教训**：服务器上的前后端服务需要分别确认是否在运行。可通过 `ps aux | grep node` 和 `ps aux | grep uvicorn` 快速排查。

---

### 问题 3：后端端口占用

**现象**：

```
ERROR: [Errno 98] Address already in use
```

**根因**：之前用 `nohup` 启动的 uvicorn 进程仍在运行，占据 8000 端口。

**解决方案**：

```bash
# 1. 找到占用端口的进程
ps aux | grep uvicorn

# 2. 终止旧进程
kill <旧PID>

# 3. 重新启动
cd /root/Tom_TranslateApp/packages/backend
nohup uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 > /tmp/backend.log 2>&1 &
```

**教训**：重启服务前，先确认旧进程已退出。可以用 `lsof -i :8000` 或 `ps aux | grep uvicorn` 检查。

---

### 问题 4：Git pull 冲突

**现象**：

```
error: Your local changes to the following files would be overwritten by merge:
        packages/backend/alembic/env.py
Please commit your changes or stash them before you merge.
Aborting
```

**根因**：之前在服务器上直接修改过 `alembic/env.py`，导致拉取新代码时产生冲突。

**解决方案**：

```bash
# 方案 A：暂存本地修改再拉取
git stash
git pull
git stash pop  # 如需要恢复本地修改

# 方案 B：放弃本地修改直接拉取
git checkout -- packages/backend/alembic/env.py
git pull
```

**教训**：
- 尽量避免在服务器上直接修改代码，所有修改应通过 Git 从本地推送。
- 如果确实需要在服务器上临时修改，应第一时间 commit 或 stash，以避免后续 pull 冲突。

---

## 服务器版本更新标准流程（总结）

基于本次排查经验，每次版本更新时在服务器上应遵循以下流程：

```bash
# ===== 1. 停止旧服务 =====
# 找到并终止后端进程
ps aux | grep uvicorn
kill <PID>

# 找到并终止前端进程（如果需要）
ps aux | grep node
kill <PID>

# ===== 2. 拉取最新代码 =====
cd /root/Tom_TranslateApp

# 检查是否有本地未提交的修改
git status

# 如果有冲突文件，先暂存
git stash

# 拉取最新代码
git pull

# 恢复暂存（如需要）
git stash pop

# ===== 3. 更新依赖 =====
# 后端 Python 依赖
cd packages/backend
uv sync

# 前端 Node 依赖
cd /root/Tom_TranslateApp
pnpm install

# ===== 4. 执行数据库迁移（如有） =====
cd packages/backend
uv run alembic upgrade head

# ===== 5. 确认 Docker 服务正常 =====
docker compose ps
# 确保 postgres、redis、libretranslate 均为 Up 状态

# ===== 6. 启动服务 =====
# 后端
cd /root/Tom_TranslateApp/packages/backend
nohup uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 > /tmp/backend.log 2>&1 &

# 前端
cd /root/Tom_TranslateApp/packages/web
nohup pnpm start > /tmp/frontend.log 2>&1 &

# ===== 7. 验证 =====
# 后端健康检查
curl http://localhost:8000/health

# 注册测试
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"verify@test.com","password":"123456"}'

# 确认 nginx 正常代理
curl -I https://tomtranslate.com
```

---

## 常用排查命令速查

| 场景 | 命令 |
|------|------|
| 查看后端进程 | `ps aux \| grep uvicorn` |
| 查看前端进程 | `ps aux \| grep node` |
| 查看端口占用 | `lsof -i :8000` 或 `ss -tlnp \| grep 8000` |
| 后端健康检查 | `curl http://localhost:8000/health` |
| 查看后端日志 | `cat /tmp/backend.log` |
| 查看 Nginx 错误日志 | `tail -50 /var/log/nginx/error.log` |
| Docker 容器状态 | `docker compose ps` |
| Docker 容器日志 | `docker logs <容器名>` |
| 数据库连接测试 | `docker exec tom_translateapp-postgres-1 psql -U translate -d translate_db -c "\dt"` |
| Git 本地修改检查 | `git status` |
